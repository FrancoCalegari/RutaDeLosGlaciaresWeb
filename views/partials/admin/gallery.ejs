<section class="admin-section">
  <h2>Galería de Imágenes</h2>
  <div id="gallery-list">
    <% images.forEach((img, index) => { 
         const realId = (typeof img.id === 'number') ? img.id : (index + 1); %>
      <div class="item" data-id="<%= realId %>">
        <input type="text" value="<%= img.src %>" class="img-src">
        <input type="text" value="<%= img.alt %>" class="img-alt">
        <button onclick="updateImageById(<%= realId %>)">✏️ Actualizar</button>
        <button onclick="deleteImageById(<%= realId %>)">🗑️ Eliminar</button>
      </div>
    <% }) %>
  </div>
  <div class="new-item">
    <input type="text" id="newImgSrc" placeholder="URL de imagen">
    <input type="text" id="newImgAlt" placeholder="Descripción">
    <button onclick="addImage()">➕ Agregar Imagen</button>
  </div>
</section>
